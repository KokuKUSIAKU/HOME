import React from "react";

const Header = () => (
  <header id="about" className="header">
    <div className="container">
      <h2 id="role-title"> Front End Developer</h2>
      <p> With ground experience in HTML, CSS, JavaScript & React<br/>
        I bring on live your project with oustanding client facing interface.<br/>
        <strong>Currently open for new opportunity!</strong></p>
    </div>
  </header>);

export default Header; 